import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className="text-xl font-semibold cursor-pointer">
      Clothes&Hair
    </Link>
  );
};

export default Logo;
